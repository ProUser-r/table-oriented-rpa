# Playwright Gmail Workflows

- `gmail_to_word.yaml`: opens unread Gmail by sender and downloads attachments.
- `gmail_send_email.yaml`: composes and sends Gmail message through browser UI.

Run example:

```bash
python docs/examples/gui_workflow_example.py
```
